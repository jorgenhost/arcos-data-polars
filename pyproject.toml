[project]
name = "arcos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.4.3",
    "ipykernel>=7.0.1",
    "jupyter>=1.1.1",
    "polars>=1.34.0",
    "pyarrow>=22.0.0",
]
